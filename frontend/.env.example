# ==============================================================================
# Frontend Environment Variables
# ==============================================================================
# These are used during local development with Vite.
# For production, set these as environment variables in Cloudflare Pages.

# ==============================================================================
# API Configuration (Development Only)
# ==============================================================================
# Optional: Direct browser -> backend connection (development only).
# For production, prefer Cloudflare Pages Functions proxy and keep secrets server-side.
VITE_API_BASE_URL=http://localhost:8010

# ==============================================================================
# Production Environment Variables (Cloudflare Pages)
# ==============================================================================
# Set these in Cloudflare Pages dashboard > Settings > Environment Variables

# Backend API URL (required)
# BACKEND_BASE_URL=https://your-backend-api.com

# API Key for backend authentication (optional, but recommended)
# BACKEND_API_KEY=your-api-key-here

# CORS: Allowed origins (required for frontend to work)
# Comma-separated list of allowed origins. Must include your frontend domain.
# Example: https://example.com,https://www.example.com
# For development: *
# ALLOWED_ORIGINS=https://example.com,https://www.example.com

# CORS: Allowed headers (optional, has safe defaults)
# Comma-separated list of allowed headers.
# Default includes: Content-Type, Authorization, X-API-Key, X-Requested-With
# ALLOWED_HEADERS=Content-Type,Authorization,X-API-Key

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# 1. Never commit actual API keys or secrets to version control
# 2. Use environment-specific values (dev, staging, production)
# 3. The Cloudflare proxy uses ALLOWED_ORIGINS to control CORS - configure it!
